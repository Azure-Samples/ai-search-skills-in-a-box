{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "resourceGroupName": {
      "value": "${AZURE_RG_NAME}"
    },
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "userIpAddress": {
      "value": "${IP_ADDRESS}"
    },
    "userPrincipalId": {
      "value": "${AZURE_PRINCIPAL_ID}"
    },
    "tags": {
      "value": {
        "Owner": "AI Team",
        "Project": "AISearchSkills",
        "Environment": "Dev",
        "Toolkit": "Bicep"
      }
    }
  }
}